"""Encoding and decoding logic for trained BPE tokenizer."""

# TODO: Implement encode(text) -> list[int]
# TODO: Implement decode(ids) -> str
# TODO: Ensure decode(encode(text)) == text round-trip property
